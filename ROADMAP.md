# ROADMAP

## Completed
- Transition from monolithic `semantic_pdf_splitter` to independent modular architecture (`app_*`).
- Implementation of spatial mapping and vision encoding adapters.

## In Progress
- CI/CD optimization: Implement dependency caching and parallel test execution (`pytest-xdist`).
- Python 3.11+ strict enforcement across all modules.

## Planned
- Integration of advanced RAG chunking heuristics based on structural AST.
