# app_vision_encoder/requirements.in
# Core dependencies for semantic manifold generation

# Tensor synthesis and testing
pytest>=8.0.0
Pillow>=10.2.0

# HTTP Socket mapping (External VLM API)
httpx>=0.27.0
respx>=0.21.0

# VRAM Allocation and Local Inference
torch>=2.2.0
transformers>=4.38.0
accelerate>=0.27.0
bitsandbytes>=0.42.0
